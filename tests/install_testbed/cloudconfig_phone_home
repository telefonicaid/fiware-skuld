#cloud-config

manage_etc_hosts: localhost

runcmd:
  - sudo iptables -I INPUT 5 -p tcp -m state --state NEW -m tcp --dport 8081 -j ACCEPT
  - echo "GatewayPorts yes" >> /etc/ssh/sshd_config
  - service sshd restart

